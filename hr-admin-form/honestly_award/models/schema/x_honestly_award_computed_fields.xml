<odoo>
    <record id="x_honestly_award_detail_x_employee_display" model="ir.model.fields">
        <field name="model">x_honestly_award_detail</field>
        <field name="model_id" ref="model_x_honestly_award_detail"/>
        <field name="name">x_employee_display</field>
        <field name="field_description">Employee Display</field>
        <field name="ttype">char</field>
        <field name="compute"><![CDATA[
def compute_value(env, model, records):
    for record in records:
        if record.x_employee_id:
            record['x_employee_display'] = record.x_employee_id.name or ''
        else:
            record['x_employee_display'] = ''
        ]]></field>
        <field name="depends">x_employee_id,x_employee_id.name</field>
        <field name="store" eval="False"/>
    </record>
</odoo>